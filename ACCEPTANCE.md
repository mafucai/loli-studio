# ACCEPTANCE — loli-studio 验收标准

> 更新：2026-09-26（build-19）
> 完整盘点见 `docs/PROGRESS.md`

## 已通过（有实测证据）

### 功能
- [x] 九步主路径可操作：设计词、出图、拆件、查重、采购、组合、模特、工厂、成本。
- [x] 历史记录可回看（设计单 / 图片 两个标签）。
- [x] 文本接口 / 图片接口分开，各自多套、可切换、可删。
- [x] 从接口自动拉取模型列表。
- [x] AI 拆件、AI 比价、AI 搜索。
- [x] 组合页估算价格（件估算成本 / 保底售价 / 建议零售价）。
- [x] 报错助手常驻角标 + 面板（真机确认显示「就绪 · 已捕获 0 条」）。

### 数据真实性
- [x] 文本设计词、拆件可切换到用户自己的兼容接口（失败回退本地并标注来源）。
- [x] 接口失败明确报错，不回退成假装成功。
- [x] 图片生成区分真实 / 演示，页面明确标注。
- [x] 采购链接能在手机上打开对应平台搜索页。
- [x] 查重标明来源；AI 结果标注「仅供参考，不是平台实测」。

### 工程
- [x] JavaScript 语法检查通过。
- [x] 本机断言 **260 项全绿**（ui-regression 111 / e2e-data 38 / full-logic-test 74 /
      models-logic-test 15 / image-logic-test 22）。
- [x] GitHub Actions 能启动并保留失败证据（build-9~19 全部 success）。
- [x] Android WebView 使用同一份前端（prototype 与 assets 逐字节一致）。
- [x] APK 由 GitHub Actions 构建（**临时签名验证包**）。
- [x] 单文件 ≤400 行、仅 store.js 读写 localStorage、渲染层不碰 DOM、事件只绑一次。

## 尚未完成

### 功能
- [ ] **避免词**：设计词下面加「不想出现的内容」，且真的传给文本 AI 让它避开。
- [ ] **电商预售流程**（图透投票、预售收定金、成团判定、补尾款发货、售后）。
- [ ] 主题风格引导（甜 / 古典 / 哥特）、柄图（印花图案）。
- [ ] 多版本样衣对比、改版留痕。

### 工程
- [ ] **正式签名**（缺 `KEYSTORE_PASSWORD`；现在只能出临时签名包，不能覆盖安装正式包）。
- [ ] http 明文流量处理（用户填 `http://` 会静默失败）。
- [ ] browser-acceptance workflow 挂死问题。
- [ ] 主人完成一次真机九步全流程验收。

## 明确不做

- 不爬小红书、抖音、淘宝、拼多多或闲鱼。
- 不把演示工厂联系方式显示成已核实信息。
- 不在 APK 内置付费 API Key。
- **App 不收款**（不接支付、不存卡号；收付款走平台或线下）。
- **App 不自动通知用户**（只生成待通知清单）。
- **App 不代替平台**（开团上架仍在平台做）。

## 交付物

1. GitHub Release 里的 APK（当前为临时签名验证包）。
2. 构建链接、校验值、改动路径和未解决问题。
3. 本机断言报告（260 项）。
