# loli-studio 洛丽塔工坊 — 项目规则

> 状态：S0 前端已由主人手机验收；仓库已建立。Android 壳、签名和 APK 尚未开始。
> 仓库：https://github.com/mafucai/loli-studio
> 编译：将来只走 GitHub Actions，本地零 Android SDK。

## 铁律

1. 已通过的 `prototype/` 是唯一视觉基准。禁止用原生控件再做一套界面。
2. 改文件前先读文件并备份；超过 500 行的文件分块处理。
3. 本地验证全绿后先给主人看，确认后才推送。
4. 单文件不超过 400 行；渲染层禁止操作 DOM；一个存储键只能有一个读写方。
5. 页面事件只在稳定容器上委托绑定一次，禁止重复绑定。
6. API Key 只存本机，不写仓库、不写日志、不上传。
7. 平台查重和采购只打开官方搜索链接。禁止爬虫、代理池和绕过平台限制。
8. 演示数据必须标明“演示”。没有真实接口结果时，禁止显示成真实结果。
9. 失败按「失败 → 根因 → 应对」记入本文档。
10. 图标将来只用脚本生成，禁止手工改 PNG。

## 模块边界

| 模块 | 可以做 | 不许做 |
|---|---|---|
| `prototype/js/core/render.js` | 数据转 HTML | 碰 DOM、改状态、读写存储 |
| `prototype/js/core/store.js` | 读写设计单和接口配置 | 碰 DOM、发网络请求 |
| `prototype/js/core/factory.js` | 生成明确标注的演示数据 | 冒充真实平台或工厂数据 |
| `prototype/js/core/actions.js` | 处理用户动作 | 直接解析业务存储 |
| `prototype/js/core/ai.js` | 调用用户配置的兼容接口 | 写死密钥、隐藏调用失败 |

## 当前阶段

- 已完成：前端原型、离线断言、数据链路测试、GitHub 空仓库首次入库。
- 未完成：治理脚本、调用蓝图、Android WebView、签名、APK。
- 已知问题：Actions 在启动作业前 0 秒失败，尚无日志，不能当成代码测试失败。

## 失败经验

| 失败 | 根因 | 应对 |
|---|---|---|
| 旧洛工坊按钮失效 | 两套脚本并存，事件重复绑定，死代码混进入口 | 新项目只保留一套入口；容器只绑定一次 |
| 首次页面会崩溃 | 中文对象键含斜杠；历史渲染括号不匹配 | 用 `node --check` 和数据链路测试拦住 |
| Actions 运行 0 秒失败 | 作业尚未启动且无日志，原因未定位 | 不谎称通过；单独排查，不在治理文件里掩盖 |
